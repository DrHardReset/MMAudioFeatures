{
  "name": "mm-audio-features-project",
  "scripts": {
    "test": "jest --testPathIgnorePatterns=integration.test.js",
    "test:integration": "jest --testPathPatterns=integration.test.js"
  },
  "type": "commonjs",
  "devDependencies": {
    "jest": "^30.0.0",
    "jest-editor-support": "^31.1.2"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/**/*.test.js"
    ]
  }
}